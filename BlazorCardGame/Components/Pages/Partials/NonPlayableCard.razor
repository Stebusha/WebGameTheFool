@using BlazorCardGame.Models

@code{
    [Parameter]
    public Card Card{get;set;}
}

@if(Card!=null)
{
    @if(!Card.IsPlayable)
    {
        <button class="game-card-button"
                enable ="return false"/>
    }
}